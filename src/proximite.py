def test ():
    return "test"

#algorithm du plus proche voisin
#def plus_proche_voisin(point, ensemble_points):
    #distances = [(p, distance_euclidienne(point, p)) for p in ensemble_points]
    #return min(distances, key=lambda x: x[1])
